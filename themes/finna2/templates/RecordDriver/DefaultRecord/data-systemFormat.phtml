<?php //Don't add START and END comments ?>
<?php if (!empty($data)): ?>
  <div class="truncate-field" data-rows="5">
    <?php foreach ($data as $field): ?>
      <?=isset($field['part']) ? $this->escapeHtml($field['part']) . ': ' : ''?>
      <?=$this->escapeHtml($field['details'] ?? '')?><br/>
    <?php endforeach; ?>
  </div>
<?php endif; ?>
